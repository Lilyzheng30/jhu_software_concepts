robots.txt was checked as it stated User-agent: * Content-Signal: search=yes,ai-train=no Allow: /, additionally /survey/ is not disabled.

Large LLM model files (`.gguf`) are not included in this repository due to GitHub file size limits.
The model is downloaded automatically on first run, or can be specified via the `MODEL_FILE`
environment variable.